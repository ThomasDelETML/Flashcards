<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flashcards - Accueil</title>
    @vite(['resources/public/css/style.css'])
</head>
<body>
    <header>
        <div class="logo">
            <a href="{{ route('home') }}">
                <img src="{{ asset('resources/images/Logo.png') }}" alt="Logo">
            </a>
        </div>
        <nav>
            <ul>
                <li><a href="{{ route('flashcards') }}">Flashcards</a></li>
                <li><a href="{{ route('home') }}">Acceuil</a></li>
                <li><a href="{{ route('login') }}">Déconnexion</a></li>
        </nav>
    </header>

    <main>
        <section class="login-form">
            <h2>Connexion</h2>
            <form action="{{ route('login') }}" method="POST">
                @csrf
                <div class="form-group">
                    <label for="username">Nom d'utilisateur</label>
                    <input type="text" id="username" name="username" required>
                </div>
                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <button type="submit" class="btn">Se connecter</button>
            </form>
        </section>
    </main>

    <footer>
        <p>&copy; 2025 Flashcards. Tous droits réservés.</p>
    </footer>
</body>
</html>
