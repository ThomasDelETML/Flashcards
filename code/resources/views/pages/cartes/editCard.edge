@component('components/layout', { title: title })
  <h3>
    {{ title }}
  </h3>
  <form action="{{ route('card.update', {id: card.id}) }}" method="post">
    @include('partials/createForm')
  </form>
  <a href="{{ route('accueil') }}" class="create-card-link mt-6">Abandonner</a>
@endcomponent