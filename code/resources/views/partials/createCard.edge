{{ csrfField() }}
<div class="field">
  @!component('components/input', { name: 'question', label: 'question', value: old('question') || (carte ? carte.question : '') })
</div>

<div class="field">
  @!component('components/input', { name: 'response', label: 'response', value: old('response') || ( carte ? carte.response : '') })
</div>

<button class="button " type="submit">Enregistrer</button>
<a href="{{ route('accueil') }}" class="button">Abandonner</a>
